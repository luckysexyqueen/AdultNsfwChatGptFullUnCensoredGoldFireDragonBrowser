# 수정본 사용 안내

이 수정본은 **`index.html`을 실제 진입점**으로 사용하는 정적 웹앱입니다. 빌드 결과물인 `dist/` 폴더를 일반 정적 호스팅, CDN, GitHub Pages 또는 모바일 WebView에 올리면 **Node.js 서버 없이** 실행됩니다.

| 기능 | 동작 방식 |
|---|---|
| 기본 사용 | 로그인 없이 즉시 시작되며, 기기 프로필·대화·메시지·첨부·프로필 이미지·프로필 문서를 IndexedDB에 저장합니다. |
| 오프라인 | 연결이 끊겨도 로컬 저장이 유지되며, 화면에 오프라인 상태가 표시됩니다. |
| 연결 복구 | 네트워크가 복구되면 연결한 계정이 있는 경우 동기화 대기열을 자동 처리합니다. |
| 프로필 편집 | 표시 이름, 소개, HTTPS 아바타 URL, 이미지 파일(최대 5MB), 문서(파일당 10MB·총 25MB·최대 10개)를 인증 없이 편집·저장·삭제할 수 있습니다. |
| 계정 연결 | 필요할 때만 메뉴의 **계정 연결·동기화**에서 Google 로그인, 이메일 로그인 또는 회원가입을 선택할 수 있습니다. |
| GGUF | 메뉴의 **로컬 GGUF 모델**에서 사용자가 선택한 `.gguf` 파일을 WebAssembly/WebGPU 런타임으로 기기에서 실행합니다. |

## 선택적 Google 로그인 및 이메일 회원가입 설정

Google 로그인과 이메일 회원가입은 기본 사용에 필요하지 않습니다. 사용하려면 메뉴의 **계정 연결·동기화**에서 대상 Supabase 프로젝트의 HTTPS URL과 공개 Anon Key를 입력합니다. 해당 Supabase 프로젝트에서는 Google 공급자를 활성화하고, 현재 앱 주소를 허용된 Redirect URL에 등록해야 합니다. 이후 Google로 연결하거나 이메일과 6자 이상 비밀번호로 회원가입할 수 있습니다.

> 공개 Anon Key만 입력하십시오. 서비스 역할 키나 다른 비밀 키를 브라우저에 입력하거나 배포하면 안 됩니다.

계정을 연결하면 기기에 쌓인 대화·메시지·프로필 변경이 동기화 대기열에 들어갑니다. 프로필 파일은 연결한 프로젝트의 `chat-files` 스토리지 버킷으로 업로드하도록 처리되어 있으므로, 버킷과 사용자별 쓰기 정책을 구성해야 실제 파일 동기화가 완료됩니다. 정책 또는 네트워크 오류가 나면 대기열은 삭제되지 않아 나중에 다시 동기화할 수 있습니다.

## 배포 및 검증

```bash
npm install
npm test
```

`npm test`는 타입 검사, 정적 검사, 프로덕션 빌드를 순서대로 실행합니다. 완료된 `dist/` 폴더만 배포하면 됩니다.

### GitHub Pages 사이트 루트 배포

`.github/workflows/main.yml`은 `dist/` **폴더 자체를 하위 경로로 올리지 않습니다.** `dist/index.html`, `dist/assets/`, `dist/sw.js`, `dist/env-config.js` 등 **dist 내부 파일이 GitHub Pages의 사이트 루트**에 직접 배포됩니다. 따라서 Pages에서 `/dist/index.html`이 아니라 `/index.html`이 실제 진입점입니다.

저장소의 **Settings → Pages → Build and deployment → Source**를 **GitHub Actions**로 지정한 뒤 `main` 브랜치에 푸시하면 됩니다. 프로젝트 Pages와 사용자/조직 Pages 모두 상대 자산 경로(`./assets/...`)를 사용하므로 동작합니다.

## 브라우저 확인 범위

무인증 즉시 진입, 게스트 메시지 송수신, 로컬 GGUF 관리 화면, 손상 GGUF 헤더 차단, 프로필 저장, 이미지·문서 파일 저장, 새로고침 후 이미지·문서 복원을 실제 정적 빌드 화면에서 확인했습니다. Google OAuth와 원격 Supabase 동기화는 사용자의 실제 Supabase URL·공개 키·Google 공급자 설정이 필요한 외부 연동이므로, 위 설정을 입력한 배포 환경에서 최종 확인해야 합니다.

## GPT 빌더 및 GGUF 기기 저장 보완

GPT 빌더는 이제 Supabase의 설정 여부나 연결 상태와 무관하게 **LocalForage/IndexedDB를 기본 저장소**로 사용합니다. 로그인하지 않아도 커스텀 GPT를 생성·수정·삭제할 수 있으며, 텍스트·코드·이미지·PDF 등의 첨부 파일은 브라우저 기기에 함께 보관됩니다. GPT 생성 후 새로고침해도 목록과 설정이 유지됩니다. 이 로컬 모드의 GPT와 첨부 파일은 다른 사용자에게 자동 공유되지 않습니다.

로컬 모델 관리 화면에서 선택한 GGUF 파일도 서버에 전송되지 않고 LocalForage에 저장됩니다. 새로고침이나 오프라인 상태에서도 저장된 모델 목록에서 다시 불러올 수 있으며, 필요 없어진 모델은 목록에서 삭제할 수 있습니다. 저장 전에는 GGUF 헤더·파일명·개별 파일 크기(2GB 이하)·브라우저 저장 여유 공간을 검사합니다. 실제 추론은 기기 메모리, 브라우저의 WebAssembly/WebGPU 지원, 모델 양자화 방식에 따라 달라지므로 작은 양자화 모델부터 사용하는 것이 안전합니다.

> 브라우저가 사이트 데이터를 삭제하거나 사용자가 브라우저 저장소를 지우면 로컬 GPT·첨부 파일·저장된 GGUF 모델도 함께 삭제됩니다. 중요한 모델 원본은 별도로 보관하세요.
